export default class ItemModel{
    constructor(code, itemName, price, quantity) {
        this.code = code;
        this.itemName = itemName;
        this.price = price;
        this.quantity = quantity;
    }
}